<component-set>
	<components>
		<component>
			<role>org.apache.maven.lifecycle.mapping.LifecycleMapping</role>
			<role-hint>exe</role-hint>
			<implementation>
				org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
			</implementation>
			<configuration>
				<phases>
					<!--use the basic jar lifecycle bindings, add additional executions 
						in here if you want anything extra to be run <process-resources> org.apache.maven.plugins:maven-resources-plugin:resources 
						</process-resources> <package> org.apache.maven.plugins:maven-jar-plugin:jar 
						</package> <install> org.apache.maven.plugins:maven-install-plugin:install 
						</install> <deploy> org.apache.maven.plugins:maven-deploy-plugin:deploy </deploy> -->
					<generate-sources>
						com.github.randomcodeorg.netmaven:netmaven:${project.version}:netmavenGenerateSources
					</generate-sources>
					<compile>
						com.github.randomcodeorg.netmaven:netmaven:${project.version}:netmavenCompile
					</compile>
					<install>
						org.apache.maven.plugins:maven-install-plugin:install
					</install>
				</phases>
			</configuration>
		</component>
		<component>
			<role>org.apache.maven.artifact.handler.ArtifactHandler</role>
			<role-hint>exe</role-hint>
			<implementation>
				org.apache.maven.artifact.handler.DefaultArtifactHandler
			</implementation>
			<configuration>
				<!--the extension used by Maven in the repository -->
				<extension>exe</extension>
				<!--the type used when specifying dependencies etc. -->
				<type>exe</type>
				<!--the packaging used when declaring an implementation of the packaging -->
				<packaging>exe</packaging>
			</configuration>
		</component>
		<component>
			<role>org.apache.maven.lifecycle.mapping.LifecycleMapping</role>
			<role-hint>dll</role-hint>
			<implementation>
				org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
			</implementation>
			<configuration>
				<phases>
					<!--use the basic jar lifecycle bindings, add additional executions 
						in here if you want anything extra to be run <process-resources> org.apache.maven.plugins:maven-resources-plugin:resources 
						</process-resources> <package> org.apache.maven.plugins:maven-jar-plugin:jar 
						</package> <install> org.apache.maven.plugins:maven-install-plugin:install 
						</install> <deploy> org.apache.maven.plugins:maven-deploy-plugin:deploy </deploy> -->
					<generate-sources>
						com.github.randomcodeorg.netmaven:netmaven:${project.version}:netmavenGenerateSources
					</generate-sources>
					<compile>
						com.github.randomcodeorg.netmaven:netmaven:${project.version}:netmavenCompile
					</compile>
					<install>
						org.apache.maven.plugins:maven-install-plugin:install
					</install>
				</phases>
			</configuration>
		</component>
		<component>
			<role>org.apache.maven.artifact.handler.ArtifactHandler</role>
			<role-hint>dll</role-hint>
			<implementation>
				org.apache.maven.artifact.handler.DefaultArtifactHandler
			</implementation>
			<configuration>
				<!--the extension used by Maven in the repository -->
				<extension>dll</extension>
				<!--the type used when specifying dependencies etc. -->
				<type>dll</type>
				<!--the packaging used when declaring an implementation of the packaging -->
				<packaging>dll</packaging>
			</configuration>
		</component>
		<component>
			<role>org.eclipse.aether.spi.connector.RepositoryConnectorFactory</role>
			<implementation>com.github.randomcodeorg.netmaven.netmaven.nuget.NugetRepositoryConnectorFactory</implementation>
			<role-hint>nuget</role-hint>
		</component>
	</components>
</component-set>